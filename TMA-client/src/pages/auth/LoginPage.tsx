import { FC } from "react";
import LoginForm from "../../components/LoginForm";

const LoginPage: FC = () => {
   return (
      <div className="">
         <LoginForm />
      </div>
   );
};

export default LoginPage;
